interface:
  display_name: "Porta Product Lifecycle"
  short_description: "Build, package, deploy, and distribute products"
  default_prompt: "Move this product through the requested lifecycle stages with separate package, deployment, distribution, and availability evidence."

policy:
  allow_implicit_invocation: true
