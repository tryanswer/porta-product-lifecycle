interface:
  display_name: "Porta Product Lifecycle"
  short_description: "Build, package, deploy, and distribute products"
  default_prompt: "Use $porta-product-lifecycle to route this concrete product outcome through one validated phase, preserve exact target and Run authority, and report only evidence observed for that phase."

policy:
  allow_implicit_invocation: true
